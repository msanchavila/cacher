from setuptools import setup

setup(
  name='caching',
  version='0.1',
  url='https://github.com/msanchavila/caching',
  packages=['cacher'],
  install_requires=['Pandas'],
  project_urls={  # Optional
    'Bug Reports': 'https://github.com/msanchavila/caching/issues',
  },
)
